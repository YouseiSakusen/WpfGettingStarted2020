<metro:MetroWindow x:Class="PrismSample.MainWindow"
				   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
				   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
				   xmlns:xb="http://schemas.microsoft.com/xaml/behaviors"
				   xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
				   xmlns:prism="http://prismlibrary.com/"
				   prism:ViewModelLocator.AutoWireViewModel="True"
				   Title="{Binding Title}" Height="350" Width="525"
				   TitleCharacterCasing="Normal"
				   WindowStartupLocation="CenterScreen">
	<xb:Interaction.Triggers>
		<xb:EventTrigger EventName="ContentRendered">
			<xb:InvokeCommandAction Command="{Binding ShowViewButtonClick}" />
		</xb:EventTrigger>
	</xb:Interaction.Triggers>
	<Grid>
		<!--<Grid.RowDefinitions>
			<RowDefinition Height="0.15*"/>
			<RowDefinition Height="0.85*"/>
		</Grid.RowDefinitions>
		<StackPanel Grid.Row="0"
					Orientation="Horizontal">
			<Button Content="View表示"
					Command="{Binding ShowViewButtonClick}"/>
		</StackPanel>-->
		<ContentControl prism:RegionManager.RegionName="ContentRegion" />
	</Grid>
</metro:MetroWindow>
